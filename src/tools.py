"""
Local non-MCP custom tools.
Add any custom inline python tools here and append them to the `tools` list.
"""

from langchain_core.tools import BaseTool

# List of custom local tools (e.g. [@tool_1, @tool_2])
tools: list[BaseTool] = []
